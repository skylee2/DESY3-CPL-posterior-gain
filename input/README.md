# Input records

This directory contains checksum records only. Place authorized chain copies outside the release tree, verify them with the corresponding checksum list, and pass their paths explicitly to the scripts.

Raw historical DES external-only chains are deliberately excluded. Public D3 files are identified separately so they are not confused with the historical external-only products.
